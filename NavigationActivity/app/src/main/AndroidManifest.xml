<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.navigationactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationActivity">
        <activity
            android:name=".exercise_one.Exercise_one"
            android:exported="true"
            android:label="@string/title_activity_exercise_one"
            android:theme="@style/Theme.NavigationActivity.NoActionBar" />
        <activity
            android:name=".nagvigationLvlOne.OldNavActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".basicNavgigation.MainActivity"
            android:exported="true" />
    </application>

</manifest>